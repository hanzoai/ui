// Svelte config for the @hanzo/ui svelte backend. The components are plain
// Svelte + TypeScript (no bundler-specific preprocessing), so the default
// config suffices; svelte-check reads this to type-check src/backends/svelte.
export default {}
