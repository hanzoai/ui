<!--
  TooltipProvider — a grouping wrapper for API parity with the shadcn backend.
  Per-tooltip open timing lives on the Tooltip root's `openDelay`, so this simply
  renders its children.
-->
<slot />
